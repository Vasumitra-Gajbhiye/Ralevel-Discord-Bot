⁠❔┃hr-updates ⁠❔┃status-update sticky message after 6
remove 📢  emoji from announce
/warn bug.
/lock-status, not working

/say, shouldn't ping @ everyone.

fix message in cert
make it so that not anyone can apply for certiifcate
merge get and apply certificates
get first and apply bottom
remove new-to-discord and merge it somewhere
server tour to welcome
remove writer cert button for now

DONE:
discord /add-sticky message limit
make sticky message resend customisable
/announce hiting limits
/mute and /timeout combine. keep timeout remove /mute-status, /unmute

PROBLEM:
dm ban appeal action (high priority)

ACCESS ISSUE:
/lock and /unlock, jr mods, sr, mod dep head and admins. not to trial mods
purge, sr mods only
/moderator-log, jr mods shouldn't be able to use it. IAL role access.
/set-nickname. only for IAL (change for higher ranks) , dc mod head and admin. general, jr and sr mods, change it for people below them
dc mod dep head should have access to all commands.
dc mod dep head should have unban
IAL access to mod logs
add roles and remove role access
